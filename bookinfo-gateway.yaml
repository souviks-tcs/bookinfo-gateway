apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: rail-ticket-dev-gateway
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: rail-ticket-dev
spec:
  hosts:
  - "*"
  gateways:
  - rail-ticket-dev-gateway
  http:
  - match:
    - uri:
        exact: /
    route:
    - destination:
        host: book-ui
        port:
          number: 3000